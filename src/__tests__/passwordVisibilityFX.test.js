describe('Given I am on the login page', () => {
  describe('Given I enter my password', () => {
    describe('when I click one time on icon eye', () => {
      test.todo('Then the password should appear');
      test.todo('Then the icon eye should change');
    });
  });
});
